import React from 'react';
import { DashboardLayout } from '../layouts/DashboardLayout';

export const AgentIssue = () => (
  <DashboardLayout pageTitle="Log Issue">
    <div className="card">Issue logging form for field agents (placeholder).</div>
  </DashboardLayout>
);
