import React from 'react';
import { DashboardLayout } from '../layouts/DashboardLayout';

export const AgentCheckIn = () => (
  <DashboardLayout pageTitle="Check In">
    <div className="card">Check-in interface for agents (placeholder).</div>
  </DashboardLayout>
);
